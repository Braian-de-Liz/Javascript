function Pares() {
     let Par = parseInt(prompt("digite um numero de sua preferência"))

     if (Par % 2 === 0 ) {
        Par == Par + 1
     } else {
        Par == Par + 1
     }

     return Par
}