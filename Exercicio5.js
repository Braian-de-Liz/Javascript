function Pares() {
     let Par = prompt(parseInt("digite um numero de sua preferência"))

     if (Par % 2 === 0 ) {
        Par == Par + 1
     } else {
        Par == Par + 1
     }

     return Par
}